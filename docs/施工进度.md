# Monstera 施工进度（批模式追踪）

> 由施工 AI 维护。每个新会话**先读本文件**，再从「当前片」接续。
> 配套文档在项目根/附件：总纲、全书、附录A、附录B、作战总图（依原文，AI 无权改）。

## 状态总览
- git：已 init，本地仓库，user=Monstera。commit 格式 `slice-N: 片名`。
- 已完成：片1（滚动主权）、片2（折叠保活）。**片3 进行中：3-A(搬骨架)✓ 3-B(Alpine+ch10 store)✓ render开关(l3-D前手)✓ 3-C-数据管道(slice-3-c-a)✓；待续 3-C-b(增量渲染核心)+3-D 收尾。**
- 尚未做：批2（片4-6.5）、批3（片7-10）。后端封存四件事本次不动。

## Git 历史（截至最新）
```
4fce6ac slice-3-c-a: 增量数据管道——新建 js/ch15.js(window.MonsteraFeed 灌 store 三域)+agentViewRender/agentPaneRender 入口旁路接入,legacy 无回归
5e9adee chore: 施工进度.md 新增高优先级修复项(停止语义漂移·根因已定位·暂不修,片3期间不碰)
6216e32 chore: 施工进度.md——片3实录(3-A/3-B/render开关)完成+3-C接续点(agentViewRender/agentPaneRender坐标·验收底线·开关纪律)
0075257 slice-3-d(前手): MONSTERA_CH.render 默认 legacy 回滚开关(ch10.js 顶部,增量注册前不切默认)
060aa48 slice-3-b: 补入 css/_style.css 的 [x-cloak]
b662482 slice-3-b: 引入本地 Alpine 3.17.1 + 新建 js/ch10.js 定义 store(三域)+x-cloak 防闪CSS
cc75dae slice-3-a: 搬骨架——css/js 外置 + 后端静态挂载单源伺服,行为分毫不变
b323c69 slice-1: 滚动主权——Agent区P4控制器+回底按钮+锁不被重置+渲染后ensure
411f1a2 slice-2: 折叠保活——chip展开态活过SSE帧(P1)+task_id隔离
```
初始全量提交：`chore: 初始化仓库`（含整棵源码快照）。

## 环境事实（新会话必备）
- git 通过 winget 装的 Git.Git 2.55。**新会话 PATH 可能无 git**，需先：
  `$env:Path=[Environment]::GetEnvironmentVariable('Path','Machine')+';'+[Environment]::GetEnvironmentVariable('Path','User')` 再 `git`。
- `cd d:\My\Monstera`。
- .gitignore 排除：backend/models/*.gguf|*.dll|*.exe、backend/*.db*|*.log|.key|.env、desktop/node_modules/、__pycache__。
- 后端启动：`cd backend; python launcher.py` → http://127.0.0.1:8765。
- **浏览器自动化验收：一律用 http://127.0.0.1:8765/（后端伺服，前端同源 /api），不用 8766。** 8766 是独立静态服务器，前端 API_BASE 是同源相对 /api，从 8766 加载会把请求打到 8766/api 恒 404，会话/模型/历史任务全空，验收必然误判。8765 根路径由后端伺服且 API 同源，功能完整。本约定为体检确认的最优验收路径，新会话必读。
- **坐标约定：文中所有「L数字-数字」一律是字符偏移，不是编辑器真实行号**。实际 index.html 仅 5335 行；style 块=字符偏移 174-89744，script 块=104487-236268（各约 1984/3044 行）。定位代码务必按内容搜索，不得按文档行号跳转。新会话必读。
- 坑：Invoke-WebRequest 读中文会乱码——验收"是否最新"用 **ASCII 指纹**（如 `fmsgOpenForTaskId`、`foldPersist`、`data-step-no`）判，别用中文。
- 沙盒可能拿 `window.fmsgOpenSteps` 判 undefined，但裸名 `fmsgOpenSteps` 可用（顶层非挂 window）。

## 每片×终稿（新会话开工前对照）
- 开工令：
  - 批1：片1✅ 片2✅ 片3(Alpine store+ch15增量渲染+章节制骨架)。
  - 批2：片4 Toast调度→片5 Truncate统一→片6 卷宗化+静默航标+tv-*删除→片6.5 间距吸附8pt。
  - 批3：片7 翻译表四泄漏点→片8 证据块+B↔C互定位→片9 文案对齐(全书第8章)→片10 章节制收尾+ch16清零。
- 每片自检：1) node --check 通过（python 抽第一枚 <script> 再 node --check _chk.js）2) 起后端实测黄金场景 3) grep 抓旁路清零 4) ch16 行数只减 5) 不动视觉数值（片6/6.5 除外）。

## 已完成片纪要（含坐标，改别处防撞）
### 片1 滚动主权
- 通用 P4 控制器在 index.html 顶部脚本区：`_scrollRegions`(Map 容器→{up,btn,thr})、`isPinnedToBottom(el,thr=80)`、`scrollBottom(el)`、`maybeAutoScroll(el)`(up=false才滚)、`bindScrollRegion(el,btn,thr)`、`updateRegionDownBtns()`。
- 开关注册：`window.MONSTERA_CH.agentScroll=true`。关闭即 agentViewRender 走旧强制贴底（else 分支）。
- ensure 模式：`ensureAgentScrollRegion(el)` 渲染后调用——**#taskView 稳定仅首次 bind；.agent-pane-stream 由 agentPaneRender 整体重建(新节点)→每次 ensure 重绑**。按钮 `.scroll-down-agent`（11px/34%灰/胶囊）。
- 调用点：agentViewRender 重建后 ensure+maybeAutoScroll；agentPaneRender 的 stream 后 ensure+maybeAutoScroll。
- CSS：.task-view/.messages/.agent-pane-stream 均加 `overflow-anchor:none`；.task-view/.agent-pane-stream 加 position:relative。
- 锁语义：上翻→up=true(锁持续到回底/点按钮)；内容增长不重置锁（maybeAutoScroll 不触发 scroll 即不重算）。
- 验收：Browser 实测 A 初始/ B 上翻5帧不拉回(pulled=false)/ C 按钮show/ D 点击贴底+up=false+btn隐藏 全过。

### 片2 折叠保活
- `window.MONSTERA_CH.foldPersist=true`；`const fmsgOpenSteps=new Set()`；`let fmsgOpenForTaskId=null`（都在脚本顶部 P4 区附近）。
- fmsgChipHtml(c,live)：输出 `data-step-no="${c.stepNo}"`；userOpen 判定在 [L4650-4652]。live 强制 open，非 live 才看 Set。
- agentViewRender 重建前捕获块 [L4752-4768]：task_id 变化→clear+跳过捕获(串任务隔离)；同task→捕获 `.open:not(.live)` 的 stepNo 写入 Set + 剪除不存在 stepNo。
- 点击handle [L5038-5049]：toggle open 同步 Set（live 跳过）。
- **已知取舍**：同 task 切走再切回，该 task 之前的展开步被清（隔离优先）。若需"往返保留"属后续优化，别擅扩。
- 验收：Browser 实测 A5chip全折叠/B展开#2#4 open=2/C8帧保活open恒2/D折叠#2剩#4/E切T2后 open=0 且 fmsgOpenSteps.size=0 复渲恒0，全过。

## 片3 计划（未开工，本会话做到的都在这）
范围极大（16+ 文件拆分 + Alpine 接入 + 增量渲染替换全量 innerHTML）。**建议本片内部再分子步，每子步 commit（临时标注 slice-3-a/b/…）,完成后合并为 slice-3 语义**。以下为执行草案：

### 3-A 搬骨架（最稳，先做）
1. vendor/ 下载 alpine.min.js（**唯一联网**，下载后即断）：版本 alpinejs@3.17.1
2. 建 css/ 与 js/ 目录。先按块整搬（非逐章，保持行为）：
   - css 整块 L174-89744 → css/_style.css（或先按总纲拆 4 章，注意 CSS 顺序敏感，覆盖依赖原序）
   - js 整块 L104487-236268 → js/ch16-legacy.js（旧实现收容所，**只增不减原则先不删**）
   - index.html 改为：<link> 引 css + 正文骨架 + <script defer> 引 vendor/alpine + js/ch16-legacy.js + js/ch10+ch15
   - **关键**：script 迁移后执行时机要等效（原内联 body 尾，当前可用 defer 或用非 defer 保序）。当前 script 用了 `const messagesEl=$('messages')` 等顶层立即执行，需 DOM 已就绪。原脚本在 body 末尾。外部脚本放 body 尾 `<script src=...></script>`（非 defer）可保顺序。
3. 验收：页面行为与改造前一致（黄金场景跑一轮），node --check 过，归零 grep。

### 3-B Alpine store（ch10）
- `Alpine.store('monstera', { sessions:[], activeTask:null, turnIndex:[], ... })` 放 ch10 顶部（DOMContentLoaded 前）。
- 三域：会话列表/当前任务元信息/轮次**索引**（序号列表）。轮次数据追加即弃，旧轮不回改。

### 3-C ch15 增量渲染
- SSE/轮询收到快照 → 增量 diff DOM，**禁止全量 innerHTML 重建**（片1/2 打底的 P4/P1 依赖"重建前捕获"，改造后需改为"store 驱动 + :key 保活"，别退回旧病）。
- 规则二：流式正文直接 textContent 追加，不碰 Alpine；结束才提交 store。
- 规则三：根元素 x-cloak + CSS `[x-cloak]{display:none}`；x-for 在 template；终态/live 用 x-show+类(x-if 无过渡)。

### 3-D 路由/开关
- `window.MONSTERA_CH.render='legacy'|'incremental'`，默认 legacy 保底，增量实现注册后按开关切。任一片出问题关开关回滚。

### 附录A验收(片3特有)
断网启动功能完整 / 250轮任务滚动展开定位不卡 / 流式CPU持平或更低 / 控制台零Alpine警告。

### 片3 已完成实录（本会话 2026-09-05，下会话先读）
- **3-A 搬骨架 ✓（cc75dae）**：index.html 从 307 行骨架 + 外置 `css/_style.css`(1983行) + `js/ch16-legacy.js`(3043行)。**配套改动（非冻结区）**：`backend/main.py` include_router 后加静态 mount 伺服 `css/js/vendor` 三目录（单源 8765，不跑 8766）。**心得**：原后端只 FileResponse index.html、无静态子目录，整搬后若不挂静态则 css/js 404 全空白——必须挂。已重启后端验证 /css /js /vendor 全部 200。3-A 行为验收：browser 8765 实测 5/6 PASS（唯一 FAIL 为宿主 `/@vite/client` 环境注入 SyntaxError，非整搬）。
- **3-B Alpine+store ✓（b662482 + 060aa48）**：index.html body 尾三枚非 defer <script>：`vendor/alpine.min.js`(3.17.1) → `js/ch10.js` → `js/ch16-legacy.js`（顺序关键，Alpine 须在 ch10 定义 store 之前就绪）。`js/ch10.js`=IIFE 定义 `Alpine.store('monstera',{sessions:[],activeTask:null,turnIndex:[]})`。`css/_style.css` 尾追加 `[x-cloak]{display:none!important}`（**注意：此补丁在第二笔 060aa48 才真正落库**，首笔 b662482 漏 stage，教训：提交后 `git show HEAD:文件 | grep` 复核）。验收：注入确认 typeof Alpine==='object'、store 三键精确、console 干净。
- **render 开关 ✓（0075257，3-D 前手）**：`js/ch10.js` 顶部已设 `window.MONSTERA_CH.render` 默认 `'legacy'`（`if (…!=='incremental') render='legacy'`），any 时点可回滚。**增量实现未注册前禁止把默认切 incremental**（红线）。
- 当前 8765 后端在跑（后台 job 日志见会话），Node v24，git 2.55。

### 3-C 已完成实录（slice-3-c-a ✓，4fce6ac）
- **数据管道就绪**：新建 `js/ch15.js`，定义 `window.MonsteraFeed(task)` + `window.MonsteraFeedBuildTurnIndex(evs)`。作用：把任务快照灌入 Alpine store `monstera` 三域——`activeTask`（id/title/status/loop_iterations/tool_call_count/final_answer/fail_reason）、`turnIndex`（由事件时间线按 STEP_STARTED 重建 `{no, toolCalls}`，旧轮不回改）、`sessions`（当前任务置顶标 active）。**纯旁路**：不改 legacy 渲染。
- 接入：`index.html` body 尾 script 顺序 = vendor/alpine → ch10.js → **ch15.js(新)** → ch16-legacy.js；`ch16-legacy.js` 的 `agentViewRender`(≈L2425) 与 `agentPaneRender`(≈L2659) 入口各插入一行 `if (window.MonsteraFeed) window.MonsteraFeed(task);`。
- 验收 PASS（browser 8765 实测 4/4）：`window.MonsteraFeed` 为 function；打开历史 Agent 任务后 `Alpine.store('monstera').activeTask.status==="completed"`、`turnIndex` 数组长 1、`sessions[0].id`===任务 id；legacy 渲染零回归（终态卡 / data-step-no chip / 执行面板 / `.scroll-down-agent` 均在）；console 干净（当前 8765 静态环境无 /@vite/client 注入）。
- 注意：`fmsgOpenSteps`/`fmsgOpenForTaskId`（片2 折叠保活 Set）**仍为 legacy 顶层变量，未迁 store**——这是 3-C-b 要做的。

### 3-C-b 待做（下会话接续点，重点）
- 目标（红线）：把 `agentViewRender` / `agentPaneRender` 的**全量 innerHTML 重建**改为 **store 驱动 + Alpine 增量**。保活语义从「重建前捕获」(fmsgOpenSteps) 迁移为「store + :key 保活」，**不得退回旧病**。流式正文用 textContent 追加不碰 Alpine；每个 `x-for` 带唯一 `:key`；`x-cloak` 加增量根容器。
- 开关纪律：`MONSTERA_CH.render` 保持默认 `legacy`（已设于 ch10.js 顶部，勿改）；增量实现注册进 `render==='incremental'` 分支。**实现注册前不切默认**；完成并实测后再考虑切默认。
- 只改 Agent 区两函数，不碰聊天 / 账单 / 模型列表（其余全量重建点本次仍不碰）。
- 精确切入点（真实行号，`js/ch16-legacy.js`）：
  - `function agentViewRender(task)` ≈ L2422（内部 `v.innerHTML` 重建 `#taskView`，含「重建前捕获」块 L2464-2479、doneHtml L2488-2519、liveHtml L2521-2523）
  - `function agentPaneRender(task)` ≈ L2656（内部 `body.innerHTML` 重建 `#agentBody`，含头部 statusLabel L2689、meter、paneSteps、footer）
  - 依赖辅助：`buildFmsgChips`(≈L2400-2421)、`fmsgChipHtml`、`paneSteps`/`paneEventRow`/`agentLiveLabel`（行号以 `rg` 现定位为准，勿按字符偏移）
- 片2 折叠保活（P1 迁移第一步）：把全局 `fmsgOpenSteps`/`fmsgOpenForTaskId` 移入 store（建议 store 增 `openSteps:{taskId,set:[]}`），agentViewRender 捕获块与 fmsgChipHtml userOpen 判断、点击 toggle 全部改为读写 store；**必须过 8 帧保活 + 切任务隔离**。
- 验收底线（改造后必须重测）：黄金场景 S4-S7 全过 + 片1/2 验收复用 = **S6 上翻 5 帧不拉回 + 展开 chip 8 帧保活(open 恒=2) + 切任务隔离(切 T2 后 open=0)**。
- 每子步 commit（slice-3-c-b/c-d/…），完成后合并 slice-3；再 3-D 收尾。
- 回归 3-A/3-B 已固话：静态挂载 main.py 挂好、script 顺序勿乱、x-cloak CSS 已铺。
- **slice-3-c-a 复核备注（非阻断，3-C-b 需遵守）**：
  1. `sessions` 当前是「最近访问置顶」语义（feed 每次 unshift 当前任务并累积历史）。3-C-b 使用前需明确 **是否要保后端会话原序**，否则可能打乱「会话列表」含义。
  2. 每帧会 **2 次 feed**（agentViewRender + agentPaneRender 各一次）→ 每帧写 store 两次。3-C-b 需**合并为单次**（如在渲染入口统一调度），避免 SSE 高频下冗余。
  3. `buildTurnIndex` 里 `TOOL_CALL_REQUESTED` 无 STEP 的兜底分支**实际不可达**（事件流 STEP 总在工具前）。**保留作防御，不要删**。

### ⏸ 3-C-b 独立验收结论（2026-09-06，判定「打回待补」）
> 独立验收 AI 结论，只记文档不改码。施工 AI 自报通过不算数。

**验收结果**：legacy 零回归**通过**；incremental 分支数据层五子检查（turnIndex 连续唯一 / 重复节点 0 / 丢失节点 0 / Alpine 警告 0 / JS 错误 0）**通过**，但**真实可用性不达标** → 判定 **「打回待补」**。

**根因**：`agentViewRender` 的增量分支在 legacy 的 `syncAgentComposer()` 调用**之前 `return`**，导致 `chat--empty` 未清除 → 触发 CSS `.app.mode-agent .chat--empty .task-view{display:none}` → `#taskView` 恒 `display:none`，中间时间线不可见、不可滚动（片 1 滚动在 incremental 不成立）。

**证据**：增量渲染后 `chat--empty=true`、`#taskView display=none`；只读调用现有 `syncAgentComposer()` 一次后 → `chat--empty=false`、`display=flex`、滚动恢复正常（bot=522、上翻不被拉回）。

**修复方向**：在增量分支中补上 `syncAgentComposer()` 调用，位置镜像 legacy 调用点（一行）。

**后续计划**：先只补这一个调用，再交另一 AI 做十二帧 + 滚动复验；确认通过后，才允许讨论是否切换默认 render。

**备注（不在本次修复范围）**：片 2「用户展开 ended chip」的折叠保活在 incremental 模式**尚未迁移**（无 `data-ext` 点击匹配、`cls` 不含 persisted-open、折叠 Set 未迁 store），属后续迁移项。

### ✅ 3-C-b 修复后复验结论（2026-09-06，条件性通过）
> 修复提交 `0e3c03f`：`agentViewRender` 增量分支在 `renderAgentViewIncremental()` 后补 `syncAgentComposer()`（镜像 legacy 渲染后调用点，置 `chat--empty=false` 使 `#taskView` 可见）。只改此一行，不改 `syncAgentComposer()` 本体，不碰 legacy / `#agentPane` / 折叠 / 滚动 / 视觉数值；`MONSTERA_CH.render` 仍默认 legacy。绿色语法检查通过。

**复验结果**：**条件性通过**。
- **incremental 十二帧**：`#taskView` 每帧 `display:flex`（可见）；turnIndex 连续唯一 `1 → 1,2 → 1,2,3`；重复节点 0；可滚动（bot=522）且上翻 `136→136` 不被拉回；Alpine 警告 0 / JS 错误 0 —— 全部通过。
- **legacy 零回归**：`defaultRender='legacy'`、每帧 feed 单次（额外增量 0）、`#taskView` 正常渲染（chips=2 / pane-turns=2）、完成卡/费用行在位、`display:flex`、0 错误 —— 通过。

**复验方法说明（字面字节级未完成）**：受浏览器持久缓存阻断，未能在浏览器中加载 post-fix 打包字节本身（见下方片 10 强制收尾项）；incremental 可见性复验采用「在内存内应用 commit 的该一行（增量渲染后调 `syncAgentComposer()`）」执行，语义与 commit 字节等同。磁盘 / 服务器 / `?rev=` 均确认送新（`server_has_fix=True`）。

**缓存问题（列入片 10 强制收尾项）**：浏览器会缓存旧版 `js/ch16-legacy.js` 的无查询 URL —— `?rev=` 可取新、普通 URL 恒取旧；`Cache-Control:no-cache`、`location.reload(true)`、服务重启、新标签页均无法清除。后续需在片 10 增加缓存兜底方案。

**备注（不在本次修复范围）**：片 2「用户展开 ended chip」折叠保活迁 store 仍是后续迁移项，非本次范围。

## ⚠️ 高优先级修复项：停止语义漂移（根因已定位，暂不修）
> 信任级问题（2026-09-05 真机亲测暴露）。查根因完成，修复方案待定，暂不修；等片 3 完成且经独立验收后再排期。

**现象**：同一次用户停止，三处文案不一致：
- 聊天流实时快照显示「任务已被用户中止」
- 执行面板顶部显示「失败」
- 刷新或重拉后聊天流也变成「任务执行失败」

**根因**（证据链）：
1. 后端 `_task_snapshot`（backend/routers/agent.py L203）只在 `status=="failed"` 时计算 `meta.terminate_kind`，该字段**不持久化**（tasks/*.json 无 meta）。
2. 任务**列表**与**详情**接口返回 `to_dict()` **裸对象**，不含 `meta`。
3. 执行面板顶部读 `task.status` 映射为「失败」（js/ch16-legacy.js L2689→L2205），不看 `meta.terminate_kind`。
4. 聊天流终态卡只在**实时快照阶段**能读到 `meta.terminate_kind==='user_interrupt'`（→「中止」），刷新/重拉后丢失 → 回退「任务执行失败」（js/ch16-legacy.js L2496）。
- 后端事件语义本身正确（终态唯一事件 `TASK_FAILED(reason=user_stopped)`，`_terminate_kind` 正确映射为 `user_interrupt`）；漂移纯为「三个读取点字段不一致 + terminate_kind 不落盘」。

**修复方向**：将 `terminate_kind` 持久化到任务落盘字段，并让所有失败类渲染（聊天流终态卡 / 执行面板顶部 / 重拉路径）统一读取同一个字段。

**状态**：根因已定位 ✅ / 修复方案待定 ⏸ / 暂不修，等片 3 完成且经独立验收后再排期。

## ✅ 片 4 / 片 5 完成与验收状态（2026-09-06）
- **片 4（Toast 统一，P3）**：`f193918`，独立验收**通过**。
- **片 5（Truncate 统一，P2）**：`8d08c07`，独立验收**通过**。
  - 原生 `title` 悬浮为**临时实现**；真正的"全量查看 / 悬浮完整"留给**片 8 证据块**。
  - 「chip 详情复制按块」为片 5 确认的**合理行为变化**（各块用自身 `data-full` 复制全文，不丢信息），验收通过。

## 后续片速记（坐标已在前面轮次定位，施工时可再确认）
- 片4 Toast：现状散装 `toast(msg)` 25+ 处，收 P3 统一队列。
- 片5 Truncate：双策略(fmsgDump 全量 vs paneDetail 4000截断)收 P2 单一策略。
- 片6：+tv-*死代码删除（tv-status-dot/tv-step/tv-turn/tv-done 这些 class 在 index.html L298-370 有 CSS 但 JS 无引用，删）。
- 片6.5：间距 42/26/22→40/24/24。
- 片7 翻译表四泄漏点：[L4709 fail_reason 原文]、[L4668 确认卡工具名]、[L2404/L3003 API错误 toast]、[L4559 翻译表兜底 `调用 ${tool}`]。
- 片9 文案：全书第8章5处（连接/发送失败/配置模型/权限/停止确认/live断线）。
- 片10 ch16 清零+final 结构。
  - ⚠️ **强制收尾项（缓存兜底）**：浏览器会缓存旧版 `js/ch16-legacy.js` 的无查询 URL（`?rev=` 可取新、普通 URL 恒取旧；`Cache-Control:no-cache` / `reload(true)` / 服务重启 / 新标签页均无法清除）。对经改动的前端静态 JS（尤其 ch16-legacy.js / ch15.js / ch10.js），片 10 需加版本号或 no-cache 兜底，保证验收/发布拉到的必是最新字节（3-C-b 修复后复验正因此仅条件性通过）。

## 片3 执行与验收对照（网络调研固化，2026-09-05）
> 开工前必读。内容来自前端拆分/Alpine 官方与社区实测，转化为本项目可操作要点。

### 拆分路线已走对（勿改）
- 后端是 FastAPI「直接发 HTML」，**不引入 Vite/Webpack/构建工具**（违背档案派，且要「构建-部署」两套流程）。
- 大文件拆分最稳做法 = **整块搬、不拆逻辑、保持行为**；AI 直接拆大文件易断函数连接。3-A 先整搬 js 到 ch16-legacy.js 再章节拆分，正确。

### 执行时机（3-A 最关键）
- 原内联 `<script>` 在 body 尾（真实行号 L2289），拆到外部**放 body 尾、非 defer** `<script src=...>`，执行时机与原完全等价。
- ⚠️ **不得改成 `type="module"`**：本项目是经典脚本 + 顶层全局 `let/const` + 全局函数，module 的延迟执行/模块作用域会断裂 `$`/`state`/`providerIcon` 等全局绑定。保持经典脚本 + 顺序。

### Alpine 接入硬性要点（3-B/3-C）
- **`x-for` 必须每个都带 `:key`（唯一稳定值）**：不带 key 时以数组下标当 key，列表整体重赋值时 Alpine 以为没变 → 更新延迟/错位（GitHub alpine #4240）。3-C 已写「store 驱动 + :key 保活」，落实到**每个** x-for（含轮次索引）。
- **`x-cloak` 必须配全局 CSS** `[x-cloak]{display:none!important}`：只加属性不给 CSS 等于无效，会闪屏(FOUC)。`!important`+`display:none` 缺一不可。
- **流式正文用 textContent 追加、不碰 Alpine**（3-C 规则二）：避免高频响应式反复 diff。
- Alpine 3.17 x-for 已优化（PR #4361）：重渲同项提速 ~95%，增/删大列表显著提升 —— 正是替换全量 innerHTML 的价值，验收时对比。
- 全局共享数据用 `Alpine.store`（三域），组件内部用 `Alpine.data`；都走本地 vendor，残网可用。

### 完成后对照备份找问题（四层验证）
| 层 | 方法 |
|---|---|
| ① 行为 | 后端自检 P5/P6/P7 + 黄金场景 10 条 |
| ② 语法/归零 | `node --check` + grep 旁路清零 + ch16 行数只减 |
| ③ DOM 结构 | 改造前后用**同一组选择器**序列化 DOM（结构+class+textContent）diff —— 片3 不许改结构 |
| ④ 视觉 | 基线截图 vs 当前截图，**固定 viewport + 禁动画 + 同浏览器同缩放**下像素 diff |

- **要点**：改造前用 browser_use 访问 **8765**，对黄金场景 S1-S10 各关键帧存「基线截图 + DOM 快照」；改造后同脚本重跑，`git diff slice2-baseline -- <拆分后文件>` 定位源码差异 + 截图/DOM 快照 diff 定位渲染差异。
- **回滚/定位**：任一异常 → `git diff slice2-baseline` 精确定位；`git checkout slice2-baseline -- <文件>` 或解压 `d:\My\Monstera-backup\20260905-slice2-baseline-src.zip` 一键还原。
- ⚠️ 视觉回归最大坑=环境不稳定（字体/GPU/动画时机），同图两跑差异先排环境再判代码。

## 交接指令（给下个会话）
1. 刷新 PATH 取 git。
2. read 本文件 → 读总纲/全书/附录A/附录B/作战总图（附件或项目根）。
3. 从 片3 3-A 开始；每子步遵守自检5条 + 提交。上下文若再满，更新本文件再停。
4. 做完片3 提交，再依开工令继续批2/批3。