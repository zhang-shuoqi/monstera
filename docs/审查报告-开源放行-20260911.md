# Monstera 开源前全量审查报告

- 日期：2026-09-11
- 审查范围：前端（index.html / css/_style.css / js/ch16-legacy.js / ch10.js / ch15.js）+ 后端（backend/ 全部非 .venv 源码）+ 仓库合规（git 历史 / 依赖 / 文档）
- 方式：只读审查，未修改任何代码。四个维度并行深审 + 全盘快速检查（git 敏感文件、依赖、外链、硬编码密钥）。
- 结论总评：**架构骨架与核心轮子质量良好，但当前不满足"放心开源"最低门槛——有 6 项 P0 阻断项必须先处理，其中安全面存在 1 条致命链路。**

## 健康度得分（10 分制）

| 维度 | 得分 | 一句话 |
|---|---|---|
| 架构 | 7.5 | 分层清晰、事件总线解耦质量高，但"全库禁直连"宣称与实践不一致 |
| 前端质量 | 8.0 | 主 XSS 路径转义到位、无调试残留、无外链；少量注入点 + 1 处性能问题 |
| 测试 | 6.0 | 0-7 阶段脚本质量高且确定性好，但游离于 pytest/CI |
| 技术债 | 6.5 | 死代码、重复导入、开发期文档混入仓库 |
| 安全 | 4.0 | 无鉴权 + 文件工具无沙箱为根因，形成本地任意文件读写链路 |
| 开源就绪 | 3.0 | LICENSE / README / gitignore 缺口 / 依赖锁定全部缺失 |

---

## P0 · 阻断开源（不处理不放行）

| # | 问题 | 证据 | 处理建议 |
|---|---|---|---|
| P0-1 | **对话隐私已进入 git 历史**：`memory/conv_1.md`、`conv_2.md` 在根提交 fa2937f 即入库；磁盘另有 untracked 的 conv_74-81 含真实对话 | `git log --oneline -- memory/conv_1.md` | 本地仓库从未推送，改写代价最低：`git filter-repo --invert-paths --path memory/` 全历史剥离；或至少 `git rm -r --cached memory/` + 书面声明 |
| P0-2 | **.gitignore 重大缺口**：`memory/`、`docs/` 截图、根目录 jpg、`css/test.jpg` 均未忽略，一次 `git add -A` 即误提交隐私与调试资产 | `git check-ignore` 实测 NOT-IGNORED | 补 .gitignore：`memory/`、`docs/**/*.png`、`docs/**/*.jpg`、`*.jpg`、`*.jpeg`、`*.png`、`desktop/dist/`、`desktop/build/` |
| P0-3 | **无 LICENSE**（第一门槛） | 全仓无 LICENSE* | 新建根 LICENSE（个人项目 MIT 或 Apache-2.0） |
| P0-4 | **无 README**，安装/启动/配置未成文 | 全仓无 README | 新建README：安装、`python launcher.py` 或 `uvicorn main:app --port 8765`、`.env` 配置、目录结构 |
| P0-5 | **后端无鉴权 + 文件工具无路径沙箱 + 用户闸默认关（致命链路）**：任意本地进程两次 HTTP 即可读写全盘任意文件、借用已存 API Key 消耗费用 | routers/agent.py:42-78、245-293；toolbox/fs_tools.py:51-224；user_gate.py:50-125；runtime.py:207；loop.py:268 | 开源即公开后门。最低要求：① fs_tools 全部路径限定显式白名单沙箱目录（禁止裸绝对路径）；② `with_gate` 默认开启不可跳过 + 路径判定去盲区；③ 本地随机 Token 鉴权中间件（启动时生成，前端注入） |
| P0-6 | **依赖无版本锁定**，无法复现构建 | backend/requirements.txt 7 个纯包名，无锁文件 | `pip freeze > requirements.lock` 入库；requirements.txt 补 `>=` 下限 |

## P1 · 高（建议随 P0 同批修）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| P1-1 | 加密密钥明文落盘 `backend/.key`，与 db 同目录 | services/encryption.py:31-51；launcher.py:31-37 | 改环境变量注入或 OS 凭据存储；至少 `.key` 已进 .gitignore（已覆盖）并设私有权限 |
| P1-2 | 后端异常字符串原样回传客户端（信息泄露） | services/openai_compat.py:243/349/439 | 客户端只回通用文案，traceback 写日志 |
| P1-3 | 提示注入 → 危险工具链路成立（objective 原样进 system prompt，模型自主调 fs 工具） | agent.py:42-78；llm_driver.py:94-125 | 与 P0-5 一并处置（工具沙箱是根治） |
| P1-4 | 前端：`agentLiveLabel` 拼接工具参数（模型可输出任意文本）直接 innerHTML | js/ch16-legacy.js:3063（label）、:2995-3000、:2559 | 改 `txt.textContent = label` |
| P1-5 | 前端：厂商/模型配置区字段未转义回显 | ch16-legacy.js:1346/1486/1489/1490/1514 | display_name/label/status/id 一律 escHtml |
| P1-6 | 前端：接口错误消息未转义回显 innerHTML | ch16-legacy.js:255/1419/1760/1782/1915/2094 | 兜底用 textContent 或 escHtml(err.message) |

## P2 · 中

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| P2-1 | Agent SSE 每帧全量重建 + 每帧重复请求 /agent/tasks（O(n²)） | ch16-legacy.js:2392-2398、:3037 | loadAgentTasks 移出逐帧路径；agentPane 增量更新 |
| P2-2 | 事件订阅者（StateProjector/MemoryTrail）正常完成任务永不 detach，内存随任务累积增长 | events.py:178-218；state_store.py | 终态时 detach 或 LRU 淘汰 |
| P2-3 | 单步超时不能真正终止线程（worker.join 超时线程仍跑）；stop_event 工具调用期间不响应 | loop.py:279-292、:218 | 协程化工具 + to_thread；文档声明限制 |
| P2-4 | 无 pytest / CI；测试强耦合私有实现 | 全部 phase*/_p6/_p7；如 _p5_smoke.py:115-121 | 迁 pytest（脚本确定性好，工程化成本低），随机 token、tmp_path |
| P2-5 | 数据库 FK 无 ondelete 级联，删除靠应用层手写顺序，易产生孤儿行 | models.py；conversations.py:92-96 | 关键外键补 ondelete=CASCADE/SET NULL |
| P2-6 | 无 Alembic，迁移为手写补列 + 特例重建，不可降级 | database.py:37-102 | 引入 Alembic 或迁移版本记录 |
| P2-7 | 多 worker 下单进程锁失效（index.json 竞态 / 全局单例裂脑） | state_store.py:115-128；main.py:180 | 明确强制单 worker（文档/启动守卫） |
| P2-8 | 错误码混用（含非常规 421） | routers/agent.py:72 | 统一 4xx 语义 |
| P2-9 | 静默吞异常无日志（损坏 settings / 快照恢复 / 读穿 index） | settings.py:74-100；state_store.py:320/360；memory_trail.py:219 | 至少补 log.warning |

## P3 · 低（清理项）

- `_p5_smoke.py:154` 与 `:175` 整段重复函数定义（copy-paste，后者覆盖前者）
- 多处重复/局部导入：state_store.py:22/28、events.py:53、runtime.py:301、agent.py:83；phase0/_p7/_p5 未使用导入
- 休眠/死分支（默认关闭且无调用方）：renderAgentViewIncremental、evidenceBlocks、mutualLocate、记忆中枢死子系统声明确认
- `local_model.py:117-126` 用 `shutil.which` 在 PATH 搜 llama 可执行文件（二进制劫持面）→ 改配置绝对路径
- 固定盐密钥派生（encryption.py:15-28）→ 文档声明"本地主权工具级加密"边界
- 记忆路径 / SQL 注入 / eval-exec / 明文 Key 回传 / 命令注入 / reload-debug：**均未发现**（已逐项排除）
- 前端 L1 图片 dataURL 直入 src（可加类型白名单）；L2 window.* 全局污染；L3 任务 run 失败静默无 toast；L4 模式 tab 缺 aria-selected

## 开源合规（S 组）

| # | 项 | 建议 |
|---|---|---|
| S3 | `desktop/dist/` 构建产物已入库（app.asar） | `git rm -r --cached desktop/dist` + ignore `desktop/build/` |
| S4 | 开发期归档文档被跟踪（docs/施工进度.md、docs/治理/*、根目录体检/功能总结等，含"施工 AI""slice-N""d:\My" 绝对路径） | 移出开源或私有归档；仅保留 LOCAL_MODEL.md + 改写 README |
| S5 | 硬编码本机路径 | docs/施工进度.md:131、功能总结:230、phase2_smoke.py:48（改 tempfile） |
| S6 | vendor/alpine.min.js（v3.17.1）无第三方许可声明 | 补 THIRD_PARTY_NOTICES.md（Alpine MIT + @vue/*） |
| N4 | `.brooks-lint-history.json` 工具状态被跟踪 | 建议忽略 |

## 修复路线图（建议顺序）

1. **放行单（P0-1~P0-6 + P1-1）**：git 历史清理 → .gitignore → LICENSE → README → 依赖锁定 → 后端文件工具白名单沙箱 + 鉴权 token + with_gate 默认开
2. **安全加固（P1-2 ~ P1-6）**：错误收敛、前端转义三处、keystore 处理
3. **工程化（P2）**：SSE 渲染优化、订阅者 detach、pytest 迁移、FK 级联、Alembic、单 worker 守卫
4. **清理（P3）**：死代码、归档文档、第三方声明

> 任何一项施工动工前，按项目约定先发「改动报告」经确认后执行。