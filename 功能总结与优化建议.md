# Monstera 项目功能总结与优化建议

> 版本：v0.2（问题清单修复后）｜日期：2026-08-18
> 定位：本地运行的 AI 模型管理控制台，当前接入 DeepSeek，支持 Mock 模式零成本测试全部功能

---

## 一、项目概述

Monstera 由两部分组成：

- **后端**：Python + FastAPI + SQLAlchemy + SQLite（WAL 模式），端口 8765，同时伺服前端页面
- **前端**：单文件 `index.html`（原生 HTML/CSS/JS，无构建依赖）

核心能力：厂商/API 管理（加密存储、主备切换、有效性检测）→ 对话（流式、上下文记忆、Markdown、故障转移、计价）→ 日志与账单 → Mock 开关。

---

## 二、文件结构

```
Monstera/
├── index.html                     # 前端单页应用（全部 UI + 交互逻辑）
├── backend/
│   ├── main.py                    # FastAPI 入口、CORS、根路径伺服前端
│   ├── config.py                  # 价格规则、超时、Mock 开关（支持热切换）
│   ├── database.py                # 连接、WAL、迁移（补列 + AUTOINCREMENT 表重建）
│   ├── models.py                  # 6 张表的 ORM 定义
│   ├── schemas.py                 # Pydantic 请求/响应模型
│   ├── .key                       # Fernet 加密密钥（自动生成，勿提交 git）
│   ├── monstera.db                # SQLite 数据库（运行时生成）
│   ├── routers/
│   │   ├── providers.py           # 厂商/API 管理、状态判定、主动检测
│   │   ├── chat.py                # 对话（流式 SSE + 非流式）
│   │   ├── conversations.py       # 对话持久化（增删改查/置顶/消息）
│   │   ├── logs.py                # 调用日志、今日账单
│   │   └── system.py              # Mock 模式查询与热切换
│   └── services/
│       ├── deepseek_service.py    # DeepSeek API 封装 + 全套 Mock 假数据
│       └── encryption.py          # API Key Fernet 加密
└── backend/venv/                  # Python 虚拟环境
```

---

## 三、功能清单

### 3.1 厂商与 API 管理

| 功能 | 说明 |
|---|---|
| 内置厂商 | 启动时自动种子 DeepSeek，无需手工配置 |
| 粘贴 API Key | 先调免费接口 `GET /models` 验证，通过后 Fernet 加密入库；首个 Key 自动为主 API |
| 主/备用 API | 圆点标记，点击切换主 API；对话时主 API 失败自动切换备用（故障转移） |
| 删除 API | 二次确认弹窗；级联删除其历史日志（防孤儿日志）；删主 API 自动提升备用；全删后停用全部模型条目 |
| 有效性主动检测 | `POST /providers/{id}/check`：逐个 Key 调 `/models`（免费），更新状态/延迟/检测时间；前端展开卡片自动触发（60 秒节流）+ 手动刷新按钮 |
| 解密失败提示 | `.key` 丢失后旧密文无法解密，UI 显示"密钥无法解密，请移除后重新粘贴" |
| 状态判定 | 无 API→未连接；主 API 检测失败或最近调用失败→异常；成功调用延迟≥2s→缓慢；否则→正常。备用救回时不再误报异常 |
| 状态显示 | 指示灯 + 文字并存，颜色：绿/黄/红/灰 |
| 延迟 | 检测时用 `/models` 耗时、对话时用真实调用耗时更新 |
| 余额 | 粘贴/检测时刷新主 API 余额；负数红色 + "已欠费"角标 |

### 3.2 模型管理

| 功能 | 说明 |
|---|---|
| 动态同步 | 从 `/models` 拉取真实模型列表入库 |
| 全量对账 | 远端存在的激活/新增；远端已下线的置 `is_active=False`（不残留过期条目）；API 全删时全部停用 |
| 添加模型按钮 | 置灰禁用，悬停提示"更多厂商接入开发中"（不误导） |

### 3.3 对话功能

| 功能 | 说明 |
|---|---|
| 对话持久化 | SQLite 存储，刷新页面不丢；列表按置顶优先 > 最近更新排序 |
| 对话管理 | 新建 / 重命名 / 置顶 / 删除（均二次确认）；首条消息自动以内容前 18 字命名 |
| 上下文记忆 | 自动携带当前对话最近 20 条消息发给模型 |
| 流式输出 | `POST /chat/stream` SSE：delta 增量帧 + meta 收尾帧（费用/耗时/对话 id）+ error 帧 + `[DONE]`；`stream_options.include_usage` 不被支持时自动降级 |
| 非流式接口 | `POST /chat` 保留兼容 |
| Markdown 渲染 | 代码块（带语言标签）、标题、列表、粗斜体、链接、引用；先 HTML 转义防 XSS |
| 计价 | 关键词规则匹配（flash / pro+reasoner / chat），匹配不到显示"未知价格"、`cost=null`，绝不落默认价 |
| 失败处理 | 所有 API 均失败时：删除已落库的用户消息（不污染上下文）+ 前端把原文恢复到输入框供修改重试 |
| 故障转移 | 主 API 失败自动尝试备用，逐个记录失败日志 |

### 3.4 日志与账单

| 功能 | 说明 |
|---|---|
| 调用日志 | 按厂商过滤，最近 200 条：时间/模型/输入输出 tokens/缓存命中/费用/延迟/成败（悬停看错误信息） |
| 今日账单 | 总调用、总花费、成功率三卡片 + 按厂商分组统计表 |
| 未知价格标注 | 日志与账单中价格未知的条目显示黄色"未知" |

### 3.5 系统与基础设施

| 功能 | 说明 |
|---|---|
| Mock 模式 | `GET/PUT /api/mock` 运行时热切换，无需重启；所有 DeepSeek 请求走假数据 |
| Mock 假数据规则 | Key 以 `sk-` 开头且 ≥8 位有效；含 `403`/`banned` 模拟未实名；含 `revoked` 首次有效、之后模拟被官网删除（测主动检测）；含 `neg`/`debt` 负余额（测欠费警示）；含 `fail`/`banned` 调用失败（测故障转移） |
| 数据库并发 | WAL 模式 + busy_timeout 15s + 外键开启 |
| id 防复用 | `user_apis`/`conversations`/`usage_logs` 建为 AUTOINCREMENT，删除后的 id 永不复用 |
| 自动迁移 | 缺列自动补（ALTER TABLE）；旧表自动保数据重建为 AUTOINCREMENT |
| 前端缓存 | 根路径响应 `Cache-Control: no-cache`，避免浏览器缓存旧版页面 |

### 3.6 API 接口一览

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/` | 前端页面 |
| GET | `/api/providers` | 厂商列表（含模型、API、状态、统计） |
| POST | `/api/providers/{id}/apis` | 粘贴并验证 API Key |
| DELETE | `/api/providers/{id}/apis/{api_id}` | 删除 API（级联日志） |
| PUT | `/api/providers/{id}/apis/{api_id}/primary` | 切换主 API |
| POST | `/api/providers/{id}/check` | 主动检测全部 Key |
| POST | `/api/chat/stream` | 流式对话（SSE） |
| POST | `/api/chat` | 非流式对话（兼容保留） |
| GET/POST | `/api/conversations` | 对话列表 / 新建 |
| PUT/DELETE | `/api/conversations/{id}` | 重命名/置顶 / 删除 |
| GET | `/api/conversations/{id}/messages` | 对话消息 |
| GET | `/api/logs` | 调用日志（可按厂商过滤） |
| GET | `/api/billing` | 今日账单 |
| GET/PUT | `/api/mock` | Mock 状态查询 / 热切换 |

### 3.7 数据库表

| 表 | 关键字段 |
|---|---|
| providers | name, display_name, base_url |
| models | provider_id, model_id, is_active |
| user_apis | api_key_encrypted, is_primary, status, balance, latency_ms, last_check_at |
| usage_logs | provider_id, api_id, tokens 四项, cost, latency_ms, success, error_message |
| conversations | title, pinned, updated_at |
| messages | conversation_id, role, content, model_id, cost, latency_ms |

---

## 四、优化建议（不新增功能，仅改进现有实现）

### A 类：正确性与体验（建议优先处理）

**A1. 流式中断会产生孤儿用户消息**
- 现状：[chat.py](backend/routers/chat.py) `chat_stream` 在流开始前先 `db.commit()` 用户消息（L204）。若用户中途关闭页面/断网，生成器被取消，用户消息已落库但没有 assistant 回复、没有日志——下次发消息时这条"半截"消息会被计入上下文。
- 建议：在生成器中捕获 `GeneratorExit`/`BaseException`，检测"未产出 meta"时删除该用户消息（复用现有全失败清理逻辑）。

**A2. 快速切换对话存在渲染竞态**
- 现状：[index.html](index.html) `loadMessages` 无请求序号守卫。快速点击两个对话时，先发出、后返回的旧响应会覆盖新对话的消息区。
- 建议：模块级自增序号，响应返回时比对序号再渲染。

**A3. 重命名/置顶会把对话"顶"到列表最前**
- 现状：[conversations.py](backend/routers/conversations.py) `update_conversation` 无条件刷新 `updated_at`，而列表按 `updated_at` 排序——重命名一个老对话会让它跳到顶部，打乱近期使用顺序。
- 建议：仅当标题或 pinned 实际变化时更新 `updated_at`，或重命名不更新排序时间。

**A4. 前端 API 地址硬编码**
- 现状：[index.html](index.html) `API_BASE = 'http://127.0.0.1:8765/api'`。页面本就由后端伺服，同源访问完全可以用相对路径；硬编码导致换端口/局域网访问即失效。
- 建议：默认用同源相对路径，检测到 `file://` 协议时再回退到 `127.0.0.1:8765`。

**A5. 语音输入/发送图片是"假按钮"**
- 现状：点击仅弹 toast"功能演示"，与此前"添加模型永远开发中"同类误导。
- 建议：与添加模型按钮一致——置灰禁用 + 悬停提示，保持 UI 诚实。

### B 类：性能（数据量增长后收益明显）

**B1. 流式渲染是 O(n²) 全量重渲**
- 现状：每个 delta 帧都对累积全文重新执行 `renderMd` 并整体替换 `innerHTML`（[index.html](index.html) `setAiContent`）。长回复（数千字）时每帧重新解析正则 + 重排 DOM，后期明显卡顿。
- 建议：按 `requestAnimationFrame` 节流（一帧最多渲一次）；或仅在收到 meta 时做最终渲染、流式期间显示纯文本。

**B2. 厂商序列化的 N+1 与重复解密**
- 现状：[providers.py](backend/routers/providers.py) `_serialize_provider` 对每个厂商发 4 条聚合查询（today_calls、hit、miss、最新日志）；每个 API 的密文被 `_decrypt` 解密 **2 次**（masked_key 与 decrypt_failed 各一次）。API 多时开销翻倍。
- 建议：解密一次存局部变量；统计合并为一条 `GROUP BY` 或按需查询。

**B3. 账单/统计在 Python 层聚合**
- 现状：[logs.py](backend/routers/logs.py) `billing` 把当日全部日志 `.all()` 拉到内存再循环累加。日志过万后内存与耗时都会上去。
- 建议：改为 SQL 聚合（`func.sum/func.count + group_by`），配合 B4 的索引。

**B4. 缺少查询索引**
- 现状：`usage_logs(provider_id, created_at)`、`usage_logs(api_id)`、`messages(conversation_id)` 均无索引，全部走全表扫描。
- 建议：建表/迁移时补这三个索引（SQLite 建索引毫秒级，零风险）。

**B5. 每次发消息成功后全量双刷新**
- 现状：前端发送完成后 `loadProviders()` + `loadConvs()` 全量拉取并重渲，侧栏与右侧卡片会闪烁。
- 建议：对话标题变化时只更新本地对应条目；统计变化时只刷新当前厂商卡片。

### C 类：健壮性与代码卫生（择机处理）

**C1. `apiFetch` 无超时**
- 现状：后端假死时前端请求永久 pending，无任何提示。
- 建议：`AbortController` + 15s 超时，超时报"后端无响应"。

**C2. 多 API 检测串行**
- 现状：[providers.py](backend/routers/providers.py) `check_provider` 逐个 Key 串行请求，某个 Key 网络慢会拖住整个检测。
- 建议：`ThreadPoolExecutor` 并行检测（接口本身是同步 def，改动小）。

**C3. 口令派生加密密钥的强度**
- 现状：[encryption.py](backend/services/encryption.py) 环境变量口令仅做单次 SHA256 派生，无盐无迭代，可被快速暴力破解。
- 建议：换 PBKDF2/scrypt 派生；本地工具风险低，至少在注释中标注权衡。

**C4. `.key` 与数据库的 git 防护**
- 现状：仓库无 `.gitignore`，`.key`（可解密全部 API Key）与 `monstera.db` 可能被误提交。
- 建议：添加 `.gitignore` 排除 `backend/.key`、`backend/monstera.db*`、`backend/venv/`。

**C5. `seed_builtin` 冗余执行**
- 现状：[providers.py](backend/routers/providers.py) 每次 `GET /providers` 都开新会话查一次厂商表，而 `main.py` 启动时已 seed 过。
- 建议：删除路由内的 `seed_builtin()` 调用，只保留启动时一次。

**C6. 非流式 `/chat` 接口维护成本**
- 现状：与流式接口重复实现故障转移/计费/回滚逻辑，两处逻辑需同步维护。
- 建议：标注 deprecated，或内部直接复用流式实现收敛到一份逻辑。

**C7. `max_tokens=2048` 硬编码**
- 现状：[deepseek_service.py](backend/services/deepseek_service.py) 固定 2048，长回复被静默截断。
- 建议：挪到 `config.py`（如 `MAX_TOKENS = 8192`），便于调整。

**C8. 后端无日志输出**
- 现状：除 uvicorn 访问日志外无应用日志，线上排错只能靠复现。
- 建议：标准 `logging` 记录调用失败、检测异常、迁移事件即可，不必引入重框架。

---

## 五、建议处理顺序

1. **A1–A5**：都是小时级改动，直接消除可见缺陷
2. **B4 + C4 + C5**：零风险、一次提交完成（索引 / gitignore / 删冗余）
3. **B1–B3、B5**：数据量上来前完成即可
4. **C 类其余**：随相关模块改动顺带处理

---

## 六、当前运行方式

```powershell
cd d:\My\Monstera\backend
.\venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8765
# 浏览器打开 http://127.0.0.1:8765/
# Mock 开关：PUT /api/mock  {"enabled": true}
```
